# CLEANORA demo

A self-contained visual demo with intentionally poor UX and a comparison state.

## Run locally

From this folder, run:

```sh
python3 -m http.server 8080
```

Then open `http://127.0.0.1:8080`.

Use the switch in the header to toggle between **Red flags** and **Good version**.
